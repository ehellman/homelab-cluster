---
description: Delegate authorized, non-destructive security testing to the security-tester agent
argument-hint: <scope / layers to test>
---

Delegate this task to the `security-tester` agent. Pass the user's test scope: `$ARGUMENTS`
