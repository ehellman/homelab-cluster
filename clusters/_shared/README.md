# Shared Cluster Resources

Cross-cluster content lives here. Components, base configs, and policies that
more than one cluster reuses are extracted into this directory one at a time,
each verified by render-diff before landing.

Populated incrementally in Phase 0b and beyond.
