# qui config storage (Ceph block)
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qui-config
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
