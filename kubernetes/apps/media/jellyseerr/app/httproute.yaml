# Internal access to Jellyseerr web UI
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jellyseerr-internal
spec:
  hostnames:
    - "jellyseerr.${SECRET_DOMAIN}"
  parentRefs:
    - kind: Gateway
      name: envoy-internal
      namespace: network
      sectionName: https
  rules:
    - backendRefs:
        - name: jellyseerr
          port: 5055
      matches:
        - path:
            type: PathPrefix
            value: /
---
# External access via Cloudflare Tunnel
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jellyseerr-external
spec:
  hostnames:
    - "seerr.${SECRET_DOMAIN}"
  parentRefs:
    - kind: Gateway
      name: envoy-external
      namespace: network
      sectionName: https
  rules:
    - backendRefs:
        - name: jellyseerr
          port: 5055
      matches:
        - path:
            type: PathPrefix
            value: /
